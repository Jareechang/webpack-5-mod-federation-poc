import * as React from 'react';
import Typography from '@material-ui/core/Typography';

const Content = () => (
    <div>
        <Typography variant="h2">Home</Typography>
        <h1>This is the home app content</h1>
    </div>
);

export default Content;
